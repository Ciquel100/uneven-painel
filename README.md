# uneven-painel

Painel do cliente da Uneven Media — página estática. Os dados vêm ao vivo da API no n8n; esta página não guarda nada além do link de acesso no próprio aparelho.

`index.html` (o app) · `tokens.css` + `components.css` (design system) · `manifest.webmanifest` + `icon.svg` + `icon-512.png` (instalar no celular).

Fonte de verdade e documentação: pasta `painel/` e `design-system/` do projeto Tech/IA (não editar aqui).
